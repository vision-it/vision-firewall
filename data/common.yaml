---
vision_firewall::ignore_purge:
  - '-m (?i:physdev)'
  - '-j DOCKER'
  - '-j DOCKER-ISOLATION'
  - '-o docker0'
  - '-i docker0'

lookup_options:
  vision_firewall::system_rules:
    merge:
      strategy: deep
      merge_hash_arrays: true
